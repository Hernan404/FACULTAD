CLAVES DE ESTE PARCIAL 

CALCULACION DE PRECIO POR HORA: 
  calculo la duracion restando hora fin - hora inicio
  multiplico la duracion por el valor en la tabla que se dispone (que es un vector)
          
procedure calcularDuracionYPrecio(var r: reservas);
begin
  r.duracion := r.hora_fin - r.hora_inicio;
  r.precio := r.duracion * TablaPrecios[r.categoria].Precio;
end;
